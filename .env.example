# ===========================================
# Spike AI Builder - Environment Configuration
# ===========================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# ===========================================

# ---- LiteLLM Configuration ----
# Your LiteLLM API key (required for summaries and multi-agent fusion)
LITELLM_API_KEY=your-litellm-api-key-here

# LiteLLM proxy URL (default provided)
LITELLM_BASE_URL=http://3.110.18.218

# Model to use for LLM calls
LITELLM_MODEL=gemini-2.5-flash

# ---- Google Analytics 4 ----
# Path to your Google Cloud service account JSON file
# The service account needs "Viewer" access to the GA4 property
GOOGLE_APPLICATION_CREDENTIALS=credentials.json

# Default GA4 property ID (can also be passed per-request in the API)
# Find this in GA4 Admin > Property Settings > Property ID
GA4_PROPERTY_ID=your-ga4-property-id

# ---- SEO Data (Screaming Frog via Google Sheets) ----
# URL of the Google Sheet containing Screaming Frog export
# Share the sheet with your service account email (Viewer access)
SEO_SHEET_URL=https://docs.google.com/spreadsheets/d/your-sheet-id/edit

# Worksheet tab to read (* = all tabs, or specify tab name)
SEO_SHEET_WORKSHEET_TITLE=*

# Set to 'true' to read all worksheet tabs
SEO_SHEET_USE_ALL_TABS=true

# Optional: specific GID of the worksheet tab
# SEO_SHEET_GID=